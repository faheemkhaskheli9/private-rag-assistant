"""Private GPT with RAG — Phase 1 ingestion package.

Phase 1 scope: get PDFs into the system safely (upload + metadata), then
chunk them (later issues). Phases 2-4 add embeddings/vector store, grounded
answer generation, and chat memory.
"""

__version__ = "0.1.0"
